import { NotapplicablePipe } from './notapplicable.pipe';

describe('NotapplicablePipe', () => {
  it('create an instance', () => {
    const pipe = new NotapplicablePipe();
    expect(pipe).toBeTruthy();
  });
});
